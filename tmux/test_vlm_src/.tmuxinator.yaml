name: navstack
root: ~/dance_ws_pedestrian_tracking

windows:
  - simulator:
      layout: main-vertical
      panes:
        - cd src/vlm_policy && python -m pytest test/ -v --tb=short --run-vllm-probe